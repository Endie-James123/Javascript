const hasNin = prompt("Do you have an NIN? (yes or no)");
const votersCard = prompt("Do you have a Voterscard? (yes or no)");
const yes = true;
const no = false;
if (hasNin || votersCard == yes) {
  console.log("you are eligible to register");
} else{hasNin || votersCard == no}(
    console.log('you are not eligible to register')
)

